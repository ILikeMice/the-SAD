{
	"compilerOptions": {
		"lib": ["dom", "dom.iterable", "dom.asynciterable", "deno.ns"]
	},

	"fmt": {
		"useTabs": true
	},

	"tasks": {
		"backend": "deno serve -A --watch backend/mod.ts",
		"build-extension": "deno run -A --watch extension/build.ts"
	}
}
