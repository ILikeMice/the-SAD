{
	"compilerOptions": {
		"lib": ["dom", "dom.iterable", "dom.asynciterable", "deno.ns"]
	},

	"fmt": {
		"useTabs": true
	},

	"tasks": {
		"backend": "deno serve -A --env --watch backend/mod.ts",
		"build-extension": "deno run -A --env --watch extension/build.ts"
	}
}
